@model IEnumerable<Vidly_Real.Models.Movie>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
<table id="t_movies" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Movie Name</th>
            <th>Genre</th>

        </tr>
    </thead>

    <tbody></tbody>

    @*@foreach (var movie in Model)
        {
            <tr>
                <td>@Html.ActionLink(movie.Name, "Details", "Movies", new { Id = movie.Id }, new { @class = "nav-link" })</td>
                <td>@movie.Genre</td>
            </tr>
        }*@
</table>

@section scripts
{
    <script>
        //t_movies datatable
        $(function () {
            $("#t_movies").DataTable({
                ajax: {
                    url: "/api/moviesAPI/",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "name"

                    },
                    {
                        data: "genre"

                    }
                ]
            });
        });
    </script>
}